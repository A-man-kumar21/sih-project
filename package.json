{
  "name": "gem-bid-compliance-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend-gateway"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:gateway": "npm --workspace backend-gateway run dev",
    "build": "npm --workspace frontend run build"
  }
}
